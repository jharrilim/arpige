import { Character } from '../characters/character';

export class Tile {

    public interactions: any[] = []; // TODO: Array of emittable events
    public characters: Character[] = [];
    constructor() {
        
    }
}
